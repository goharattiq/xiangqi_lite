/* eslint-disable import/prefer-default-export */
export const config = {
  headers: {
    'Content-Type': 'application/json',
  },
};

export const BASE_URL = 'https://api.xiangqi-dev.arbisoft.com';
